<h1>Listing ideas</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Descricao</th>
    <th>Link</th>
    <th>Keywords</th>
    <th>Repositorio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ideas.each do |idea| %>
  <tr>
    <td><%= idea.nome %></td>
    <td><%= idea.descricao %></td>
    <td><%= idea.link %></td>
    <td><%= idea.keywords %></td>
    <td><%= idea.repositorio %></td>
    <td><%= link_to 'Show', event_idea_path(@event, idea) %></td>
    <td><%= link_to 'Edit', edit_event_idea_path(@event, idea) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Idea', new_event_idea_path %>
